============================= test session starts ==============================
platform linux -- Python 3.12.3, pytest-8.3.4, pluggy-1.6.0 -- /home/rldc/RLdC_AiNalyzator/RLdC_AiNalyzator/venv/bin/python3
cachedir: .pytest_cache
rootdir: /home/rldc/RLdC_AiNalyzator/RLdC_AiNalyzator
configfile: pytest.ini
plugins: asyncio-0.25.2, anyio-4.13.0
asyncio: mode=Mode.STRICT, asyncio_default_fixture_loop_scope=function
collecting ... collected 43 items

tests/test_trading_signal_engine.py::test_klines_to_df_preserves_quote_volume_and_trades PASSED [  2%]
tests/test_trading_signal_engine.py::TestTrendScore::test_strong_uptrend PASSED [  4%]
tests/test_trading_signal_engine.py::TestTrendScore::test_moderate_uptrend_no_200 PASSED [  6%]
tests/test_trading_signal_engine.py::TestTrendScore::test_downtrend PASSED [  9%]
tests/test_trading_signal_engine.py::TestTrendScore::test_no_data PASSED [ 11%]
tests/test_trading_signal_engine.py::TestMomentumScore::test_rsi_optimal PASSED [ 13%]
tests/test_trading_signal_engine.py::TestMomentumScore::test_rsi_overbought PASSED [ 16%]
tests/test_trading_signal_engine.py::TestMomentumScore::test_rsi_oversold PASSED [ 18%]
tests/test_trading_signal_engine.py::TestMomentumScore::test_macd_negative PASSED [ 20%]
tests/test_trading_signal_engine.py::TestVolumeScore::test_high_volume PASSED [ 23%]
tests/test_trading_signal_engine.py::TestVolumeScore::test_low_volume PASSED [ 25%]
tests/test_trading_signal_engine.py::TestVolumeScore::test_none_values PASSED [ 27%]
tests/test_trading_signal_engine.py::TestHtfAgreement::test_strong_uptrend_4h PASSED [ 30%]
tests/test_trading_signal_engine.py::TestHtfAgreement::test_downtrend_4h PASSED [ 32%]
tests/test_trading_signal_engine.py::TestHtfAgreement::test_no_data PASSED [ 34%]
tests/test_trading_signal_engine.py::TestHtfAgreement::test_ema_up_but_rsi_weak PASSED [ 37%]
tests/test_trading_signal_engine.py::TestRegimeScore::test_price_in_lower_half PASSED [ 39%]
tests/test_trading_signal_engine.py::TestRegimeScore::test_price_at_upper_band PASSED [ 41%]
tests/test_trading_signal_engine.py::TestRegimeScore::test_narrow_bands PASSED [ 44%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_no_klines_returns_invalid PASSED [ 46%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_insufficient_klines PASSED [ 48%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_low_quote_volume_marks_observe_only FAILED [ 51%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_shallow_orderbook_blocks_buy FAILED [ 53%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_missing_klines_count_uses_context_fallback FAILED [ 55%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_fast_timeframe_requests_enough_history FAILED [ 58%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_no_atr PASSED [ 60%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_no_price PASSED [ 62%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_spread_too_wide_blocks_buy PASSED [ 65%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_spread_ok_passes FAILED [ 67%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_rsi_overbought_blocks_buy PASSED [ 69%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_rsi_below_75_does_not_block FAILED [ 72%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_htf_downtrend_blocks_buy PASSED [ 74%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_htf_check_disabled_allows_entry FAILED [ 76%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_htf_uptrend_allows_entry FAILED [ 79%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_downtrend_on_entry_tf_blocks_buy FAILED [ 81%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_fast_tf_disagrees_blocks_buy FAILED [ 83%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_negative_edge_blocks_buy FAILED [ 86%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_good_atr_positive_edge FAILED [ 88%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_score_too_low_blocks_buy FAILED [ 90%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_confidence_too_low_blocks_buy FAILED [ 93%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_valid_signal_passes FAILED [ 95%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_valid_signal_has_correct_fields FAILED [ 97%]
tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_external_confidence_overrides_heuristic FAILED [100%]

=================================== FAILURES ===================================
_______ TestEvaluateEntrySignal.test_low_quote_volume_marks_observe_only _______

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec2960>

    def test_low_quote_volume_marks_observe_only(self):
        cfg = _default_cfg(require_volume_confirmation=False)
        ctx = _make_good_ctx()
        ctx["volume_24h_quote"] = 42_000.0
        ctx["trade_count"] = 650.0
        ctx["volume_ratio"] = 1.7
>       result = self._call(cfg, ctx_entry=ctx)

tests/test_trading_signal_engine.py:286: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582632752'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582617760'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
__________ TestEvaluateEntrySignal.test_shallow_orderbook_blocks_buy ___________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec2b10>

    def test_shallow_orderbook_blocks_buy(self):
        cfg = _default_cfg(min_depth_to_order_ratio=25.0)
        ctx = _make_good_ctx()
    
        mock_bc = MagicMock()
        mock_bc.get_book_ticker.return_value = {
            "bidPrice": "99.99",
            "askPrice": "100.01",
        }
        mock_bc.get_orderbook.return_value = {
            "symbol": "BTCUSDC",
            "bids": [[99.95, 1.0]],
            "asks": [[100.05, 1.0]],
            "timestamp": 1,
        }
    
        with patch("backend.analysis.get_live_context") as mock_ctx:
            mock_ctx.return_value = ctx
            db = MagicMock()
>           result = evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)

tests/test_trading_signal_engine.py:312: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582529168'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582614208'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
___ TestEvaluateEntrySignal.test_missing_klines_count_uses_context_fallback ____

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec2cc0>

    def test_missing_klines_count_uses_context_fallback(self):
        cfg = _default_cfg()
        ctx = _make_good_ctx()
        ctx.pop("klines_count", None)
>       result = self._call(cfg, ctx_entry=ctx)

tests/test_trading_signal_engine.py:322: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582669024'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582537712'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
_____ TestEvaluateEntrySignal.test_fast_timeframe_requests_enough_history ______

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec2e70>

    def test_fast_timeframe_requests_enough_history(self):
        cfg = _default_cfg()
        seen_limits = {}
    
        def _mock_get_live_context(db, symbol, timeframe="1h", limit=200):
            seen_limits[timeframe] = limit
            if timeframe == cfg.htf_timeframe:
                return _make_htf_ctx()
            if timeframe == cfg.fast_timeframe:
                return _make_fast_ctx()
            return _make_good_ctx()
    
        mock_bc = MagicMock()
        mock_bc.get_book_ticker.return_value = {"bidPrice": "99.95", "askPrice": "100.05"}
    
        with patch("backend.analysis.get_live_context", side_effect=_mock_get_live_context):
>           result = evaluate_entry_signal(MagicMock(), "BTCUSDC", cfg, binance_client=mock_bc)

tests/test_trading_signal_engine.py:342: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393583018816'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393583006752'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
________________ TestEvaluateEntrySignal.test_spread_ok_passes _________________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec35c0>

    def test_spread_ok_passes(self):
        """Małe spread nie blokuje."""
        cfg = _default_cfg(max_allowed_spread_pct=0.30)
        ctx = _make_good_ctx()
        mock_bc = MagicMock()
        mock_bc.get_book_ticker.return_value = {
            "bidPrice": "99.99",
            "askPrice": "100.01",
        }
    
        with patch("backend.analysis.get_live_context") as mock_ctx:
            mock_ctx.return_value = ctx
            db = MagicMock()
>           result = evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)

tests/test_trading_signal_engine.py:395: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393581189040'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393581185872'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
___________ TestEvaluateEntrySignal.test_rsi_below_75_does_not_block ___________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec3920>

    def test_rsi_below_75_does_not_block(self):
        cfg = _default_cfg()
        ctx = _make_good_ctx(rsi=74.9)
>       result = self._call(cfg, ctx_entry=ctx)

tests/test_trading_signal_engine.py:414: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582660432'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582662304'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
_________ TestEvaluateEntrySignal.test_htf_check_disabled_allows_entry _________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec3890>

    def test_htf_check_disabled_allows_entry(self):
        """Gdy require_htf_trend_agreement=False, downtrend 4h nie blokuje."""
        cfg = _default_cfg(require_htf_trend_agreement=False)
        ctx = _make_good_ctx()
        htf = _make_htf_ctx(ema_20=90.0, ema_50=100.0, rsi=35.0)
>       result = self._call(cfg, ctx_entry=ctx, ctx_htf=htf)

tests/test_trading_signal_engine.py:433: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393583008000'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582606816'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
____________ TestEvaluateEntrySignal.test_htf_uptrend_allows_entry _____________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec32f0>

    def test_htf_uptrend_allows_entry(self):
        """Silny uptrend 4h nie blokuje."""
        cfg = _default_cfg(require_htf_trend_agreement=True)
        ctx = _make_good_ctx()
        htf = _make_htf_ctx(ema_20=110.0, ema_50=100.0, rsi=65.0)
>       result = self._call(cfg, ctx_entry=ctx, ctx_htf=htf)

tests/test_trading_signal_engine.py:441: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582621136'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582628624'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
________ TestEvaluateEntrySignal.test_downtrend_on_entry_tf_blocks_buy _________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec2de0>

    def test_downtrend_on_entry_tf_blocks_buy(self):
        """EMA20 < EMA50 na 1h = brak sygnału BUY."""
        cfg = _default_cfg()
        ctx = _make_good_ctx(ema_20=95.0, ema_50=100.0)  # EMA20 < EMA50 = downtrend
>       result = self._call(cfg, ctx_entry=ctx)

tests/test_trading_signal_engine.py:450: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393583098192'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582662496'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
__________ TestEvaluateEntrySignal.test_fast_tf_disagrees_blocks_buy ___________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec28d0>

    def test_fast_tf_disagrees_blocks_buy(self):
        """15m w downtrendzie + 1h słaby trend = brak BUY."""
        cfg = _default_cfg()
        ctx = _make_good_ctx(ema_20=101.0, ema_50=100.0)  # słaby trend 1h
        fast = _make_fast_ctx(ema_20=97.0, ema_50=100.0)  # downtrend 15m
>       result = self._call(cfg, ctx_entry=ctx, ctx_fast=fast)

tests/test_trading_signal_engine.py:459: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393583029232'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393583106592'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
____________ TestEvaluateEntrySignal.test_negative_edge_blocks_buy _____________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec2360>

    def test_negative_edge_blocks_buy(self):
        """ATR zbyt mały → ujemny edge po kosztach."""
        # ATR = 0.01 na price=100 → expected_move = 0.01% << koszty
        cfg = _default_cfg(
            min_net_edge_pct=0.10,
            taker_fee_pct=0.10,  # round-trip ~0.20%
            slippage_bps=5.0,
        )
        ctx = _make_good_ctx(atr=0.001, price=100.0)  # ATR/price = 0.001%
>       result = self._call(cfg, ctx_entry=ctx)

tests/test_trading_signal_engine.py:476: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582630208'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393583108032'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
_____________ TestEvaluateEntrySignal.test_good_atr_positive_edge ______________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec1cd0>

    def test_good_atr_positive_edge(self):
        """Duży ATR → edge pozytywny."""
        cfg = _default_cfg(
            min_net_edge_pct=0.10,
            min_expected_rr=1.0,
        )
        ctx = _make_good_ctx(atr=3.0, price=100.0)  # ATR/price = 3%
>       result = self._call(cfg, ctx_entry=ctx)

tests/test_trading_signal_engine.py:487: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582660960'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582620800'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
____________ TestEvaluateEntrySignal.test_score_too_low_blocks_buy _____________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec17c0>

    def test_score_too_low_blocks_buy(self):
        cfg = _default_cfg(min_entry_score=0.99)  # niewyosiągalny próg
        ctx = _make_good_ctx()
>       result = self._call(cfg, ctx_entry=ctx)

tests/test_trading_signal_engine.py:498: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393581872608'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393581190528'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
__________ TestEvaluateEntrySignal.test_confidence_too_low_blocks_buy __________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec3d10>

    def test_confidence_too_low_blocks_buy(self):
        cfg = _default_cfg(min_signal_confidence=0.99)
        ctx = _make_good_ctx()
>       result = self._call(cfg, ctx_entry=ctx)

tests/test_trading_signal_engine.py:505: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582526432'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582604560'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
_______________ TestEvaluateEntrySignal.test_valid_signal_passes _______________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77ec3ec0>

    def test_valid_signal_passes(self):
        """Idealne warunki → sygnał VALID."""
        cfg = _default_cfg(
            min_entry_score=0.40,
            min_signal_confidence=0.40,
            min_net_edge_pct=0.05,
            min_expected_rr=0.5,
            require_volume_confirmation=False,
            require_htf_trend_agreement=False,
            min_liquidity_score=0.0,
        )
        ctx = _make_good_ctx(
            price=100.0, atr=2.0, klines_count=200,
            rsi=52.0, ema_20=102.0, ema_50=99.0, ema_200=90.0,
            macd_hist=0.5, volume_ratio=2.0,
        )
        fast = _make_fast_ctx(ema_20=102.0, ema_50=99.0)
>       result = self._call(cfg, ctx_entry=ctx, ctx_fast=fast)

tests/test_trading_signal_engine.py:528: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582612192'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393581876640'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
_________ TestEvaluateEntrySignal.test_valid_signal_has_correct_fields _________

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77f180b0>

    def test_valid_signal_has_correct_fields(self):
        """Valid signal ma wypełnione wszystkie ważne pola."""
        cfg = _default_cfg(
            min_entry_score=0.30,
            min_signal_confidence=0.30,
            min_net_edge_pct=0.01,
            min_expected_rr=0.3,
            require_htf_trend_agreement=False,
            require_volume_confirmation=False,
            min_liquidity_score=0.0,
        )
        ctx = _make_good_ctx(price=50000.0, atr=1000.0)
        fast = _make_fast_ctx()
>       result = self._call(cfg, ctx_entry=ctx, ctx_fast=fast)

tests/test_trading_signal_engine.py:549: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_trading_signal_engine.py:262: in _call
    return evaluate_entry_signal(db, "BTCUSDC", cfg, binance_client=mock_bc)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393581190288'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582657312'>
external_confidence = None

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
_____ TestEvaluateEntrySignal.test_external_confidence_overrides_heuristic _____

self = <test_trading_signal_engine.TestEvaluateEntrySignal object at 0x720b77f18260>

    def test_external_confidence_overrides_heuristic(self):
        cfg = _default_cfg(
            min_signal_confidence=0.90,  # wysoki próg
        )
        ctx = _make_good_ctx()
        # external_confidence = 0.95 → powinien przejść confidence gate
        with patch("backend.analysis.get_live_context") as mock_ctx:
            mock_ctx.return_value = ctx
            db = MagicMock()
            mock_bc = MagicMock()
            mock_bc.get_book_ticker.return_value = {"bidPrice": "99.99", "askPrice": "100.01"}
>           result = evaluate_entry_signal(
                db, "BTCUSDC", cfg,
                binance_client=mock_bc,
                external_confidence=0.95,
            )

tests/test_trading_signal_engine.py:570: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <MagicMock id='125393582620752'>, symbol = 'BTCUSDC'
cfg = TradeConfig(trading_mode='demo', allow_live_trading=False, demo_trading_enabled=True, paper_mode=False, execution_enab...=45, max_signal_age_sec=3600, pending_order_cooldown_sec=300, binance_rejection_cooldown_sec=600, inflight_ttl_sec=120)
meta = None, binance_client = <MagicMock id='125393582629872'>
external_confidence = 0.95

    def evaluate_entry_signal(
        db: Session,
        symbol: str,
        cfg,                          # TradeConfig
        meta=None,                    # SymbolMeta (opcjonalne — do spread check)
        binance_client=None,          # do bookTicker / avg_price
        external_confidence: Optional[float] = None,  # z AI provider
    ) -> EntrySignalResult:
        """
        Główna funkcja oceny sygnału wejścia.
    
        Kroki:
          1. Pobierz wskaźniki z 3 timeframes (fast, entry, htf)
          2. Sprawdź spread
          3. Oblicz 6 komponentów score
          4. Sprawdź hard gates (volume, liquidity, htf, RSI overbought)
          5. Sprawdź expected value po kosztach
          6. Zwróć EntrySignalResult
    
        Args:
            db:                   Session bazy danych
            symbol:               symbol np. 'BTCUSDC'
            cfg:                  TradeConfig
            meta:                 SymbolMeta z symbol_filter
            binance_client:       do bookTicker
            external_confidence:  confidence z AI/ML (jeśli None → liczymy heurystycznie)
        """
        from backend.analysis import get_live_context
    
        result = EntrySignalResult(symbol=symbol, is_valid=False)
    
        # ── 1. Pobierz wskaźniki ──────────────────────────────────────────────
        entry_tf = cfg.entry_timeframe      # 1h
        htf_tf = cfg.htf_timeframe          # 4h
        fast_tf = cfg.fast_timeframe        # 15m
    
        try:
            ctx_entry = get_live_context(db, symbol, timeframe=entry_tf, limit=200) or {}
        except Exception as exc:
            logger.debug("evaluate_entry_signal: get_live_context(%s,%s) error: %s", symbol, entry_tf, exc)
            ctx_entry = {}
    
        try:
            ctx_htf = get_live_context(db, symbol, timeframe=htf_tf, limit=100) or {}
        except Exception:
            ctx_htf = {}
    
        try:
            ctx_fast = get_live_context(db, symbol, timeframe=fast_tf, limit=100) or {}
        except Exception:
            ctx_fast = {}
    
        if not ctx_entry:
            result.reason_code = "no_klines_data"
            result.reason_pl = _REASON_PL["no_klines_data"]
            return result
    
        # Sprawdź minimalną liczbę klines
        klines_count = _safe_float(ctx_entry.get("klines_count"), 200 if ctx_entry else 0)
        if klines_count < 50:
            result.reason_code = "insufficient_klines"
            result.reason_pl = _REASON_PL["insufficient_klines"]
            result.details["klines_count"] = klines_count
            return result
    
        # ATR (obowiązkowe — bez ATR nie możemy obliczyć TP/SL)
        atr = _safe_float(ctx_entry.get("atr"))
        if atr <= 0:
            result.reason_code = "no_atr"
            result.reason_pl = _REASON_PL["no_atr"]
            return result
    
        # Cena bieżąca
        price = _safe_float(ctx_entry.get("close") or ctx_entry.get("price"))
        if price <= 0:
            result.reason_code = "no_price"
            result.reason_pl = _REASON_PL["no_price"]
            return result
    
        result.atr = atr
        result.entry_price = price
    
        # ── 2. Spread bid/ask ────────────────────────────────────────────────
        spread_pct = 0.0
        if binance_client:
            spread_pct = _get_spread_pct(binance_client, symbol)
        result.spread_pct = spread_pct
    
        if cfg.max_allowed_spread_pct > 0 and spread_pct > cfg.max_allowed_spread_pct:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_pct"] = round(spread_pct, 4)
            result.details["max_allowed_spread_pct"] = cfg.max_allowed_spread_pct
            return result
    
        spread_bps = spread_pct * 100.0
        max_slippage_bps = _safe_float(getattr(cfg, "max_slippage_bps", 0.0), 0.0)
        if max_slippage_bps > 0 and spread_bps > max_slippage_bps:
            result.reason_code = "spread_too_wide"
            result.reason_pl = _REASON_PL["spread_too_wide"]
            result.details["spread_bps"] = round(spread_bps, 4)
            result.details["max_slippage_bps"] = max_slippage_bps
            return result
    
        # ── 3. Komponenty score ───────────────────────────────────────────────
        ema_20 = ctx_entry.get("ema_20")
        ema_50 = ctx_entry.get("ema_50")
        ema_200 = ctx_entry.get("ema_200")
        rsi = ctx_entry.get("rsi")
        macd_hist = ctx_entry.get("macd_histogram") or ctx_entry.get("macd_hist")
        macd_sig = ctx_entry.get("macd_signal")
        volume_ratio = ctx_entry.get("volume_ratio")
        volume_spike = ctx_entry.get("volume_spike_ratio")
        bb_upper = ctx_entry.get("bb_upper")
        bb_lower = ctx_entry.get("bb_lower")
        bb_mid = ctx_entry.get("bb_middle") or ctx_entry.get("bb_mid")
        quote_volume_24h = _safe_float(ctx_entry.get("volume_24h_quote") or ctx_entry.get("quote_volume"))
        trades_1h = _safe_float(ctx_entry.get("trade_count") or ctx_entry.get("num_trades"))
        volume_ratio_v = _safe_float(volume_ratio, 1.0)
    
        market_block: Optional[Tuple[int, str, str, Dict[str, Any]]] = None
    
        def _mark_market_block(priority: int, reason_code: str, details: Dict[str, Any]) -> None:
            nonlocal market_block
            candidate = (priority, reason_code, _REASON_PL[reason_code], details)
            if market_block is None or priority < market_block[0]:
                market_block = candidate
    
        # Pozycja w pasmie Bollingera
        bb_pos = None
        bb_width = None
        if bb_upper and bb_lower and bb_mid and price > 0:
            band_range = _safe_float(bb_upper) - _safe_float(bb_lower)
            if band_range > 0:
                bb_pos = (price - _safe_float(bb_lower)) / band_range
                bb_width = band_range / _safe_float(bb_mid)
    
        trend_score = _compute_trend_score(ema_20, ema_50, ema_200, price)
        momentum_score = _compute_momentum_score(rsi, macd_hist, macd_sig)
        volume_score = _compute_volume_score(volume_ratio, volume_spike)
    
        effective_min_quote_volume_trade = _effective_quote_volume_trade_threshold(
            cfg,
            volume_ratio=volume_ratio_v,
            spread_pct=spread_pct,
        )
    
        if quote_volume_24h < effective_min_quote_volume_trade:
            _mark_market_block(
                1,
                "volume_below_trade_threshold",
                {
                    "quote_volume_24h": round(quote_volume_24h, 4),
                    "min_quote_volume_trade": round(_safe_float(getattr(cfg, "min_quote_volume_trade", 0.0), 0.0), 4),
                    "effective_min_quote_volume_trade": round(effective_min_quote_volume_trade, 4),
                    "volume_ratio": round(volume_ratio_v, 3),
                    "spread_bps": round(spread_bps, 4),
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        if cfg.require_volume_confirmation and volume_ratio_v < cfg.volume_ratio_min:
            _mark_market_block(
                3,
                "volume_too_low",
                {
                    "volume_ratio": round(volume_ratio_v, 3),
                    "required": cfg.volume_ratio_min,
                    "market_mode": "observe_only",
                    "tradable": False,
                },
            )
    
        min_depth_to_order_ratio = _safe_float(getattr(cfg, "min_depth_to_order_ratio", 0.0), 0.0)
        orderbook_depth_bps = _safe_float(getattr(cfg, "orderbook_depth_bps", 20.0), 20.0)
        depth_to_order_ratio = 0.0
        orderbook_quote_depth = 0.0
        if min_depth_to_order_ratio > 0:
            depth_to_order_ratio, orderbook_quote_depth = _get_orderbook_depth_ratio(
                binance_client,
                symbol,
                price,
                orderbook_depth_bps,
                _safe_float(getattr(cfg, "min_buy_notional", 60.0), 60.0),
            )
            if depth_to_order_ratio < min_depth_to_order_ratio:
                _mark_market_block(
                    2,
                    "orderbook_depth_too_low",
                    {
                        "orderbook_depth_bps": orderbook_depth_bps,
                        "orderbook_quote_depth": round(orderbook_quote_depth, 4),
                        "depth_to_order_ratio": round(depth_to_order_ratio, 4),
                        "required_depth_to_order_ratio": min_depth_to_order_ratio,
                        "market_mode": "observe_only",
                        "tradable": False,
                    },
                )
    
        # HTF agreement
        htf_ema_20 = ctx_htf.get("ema_20")
        htf_ema_50 = ctx_htf.get("ema_50")
        htf_rsi = ctx_htf.get("rsi")
        htf_score = _compute_htf_agreement(htf_ema_20, htf_ema_50, htf_rsi, price)
    
        if cfg.require_htf_trend_agreement and htf_score < 0.35:
            result.reason_code = "htf_trend_disagrees"
            result.reason_pl = _REASON_PL["htf_trend_disagrees"]
            result.details["htf_score"] = round(htf_score, 3)
            result.details["htf_ema_20"] = htf_ema_20
            result.details["htf_ema_50"] = htf_ema_50
            result.details["htf_rsi"] = htf_rsi
            return result
    
        # RSI overbought — nie wchodzimy przy RSI > 75
        rsi_v = _safe_float(rsi, 50.0)
        if rsi_v > 75.0:
            result.reason_code = "rsi_overbought"
            result.reason_pl = _REASON_PL["rsi_overbought"]
            result.details["rsi"] = round(rsi_v, 2)
            return result
    
        liquidity_score = _compute_liquidity_score(
>           volume_24h_quote=volume_24h,
            trade_count_1h=trades_1h,
            spread_pct=spread_pct,
            min_volume_24h=50_000.0,   # 50k USDC/EUR minimum
            min_trades_1h=50.0,
        )
E       NameError: name 'volume_24h' is not defined

backend/trading/signal_engine.py:613: NameError
=========================== short test summary info ============================
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_low_quote_volume_marks_observe_only
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_shallow_orderbook_blocks_buy
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_missing_klines_count_uses_context_fallback
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_fast_timeframe_requests_enough_history
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_spread_ok_passes
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_rsi_below_75_does_not_block
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_htf_check_disabled_allows_entry
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_htf_uptrend_allows_entry
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_downtrend_on_entry_tf_blocks_buy
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_fast_tf_disagrees_blocks_buy
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_negative_edge_blocks_buy
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_good_atr_positive_edge
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_score_too_low_blocks_buy
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_confidence_too_low_blocks_buy
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_valid_signal_passes
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_valid_signal_has_correct_fields
FAILED tests/test_trading_signal_engine.py::TestEvaluateEntrySignal::test_external_confidence_overrides_heuristic
======================== 17 failed, 26 passed in 4.69s =========================
